const express = require('express');
const router = express.Router();
// Reviews are handled via /api/products/:id/reviews
// This file exists for future standalone review endpoints
module.exports = router;
